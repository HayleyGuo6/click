import type {Session,Turn} from '@/lib/types';
function Path({title,turns}:{title:string;turns:Turn[]}){return <div><strong>{title}</strong>{turns.map((t,i)=>{const chosen=t.attempts.find(a=>a.id===t.selectedId)||t.attempts.at(-1);return <section className="comparison-turn" key={t.id}><small>第 {i+1} 轮 · 对方</small><p>{t.opponent}</p>{chosen&&<><small>我 · {chosen.assisted?'参考后重答':'首次回答'}</small><p>{chosen.text}</p></>}</section>;})}</div>;}
export default function BranchComparison({session}:{session:Session}){return <div className="branch-content">{session.branches.map((branch,i)=><details key={i}><summary>另一条路 {i+1}</summary><div className="branch-compare"><Path title="当时的回应与后续" turns={branch.turns}/><Path title="现在选用的回应与后续" turns={session.turns}/></div></details>)}</div>;}
