import {body,db,owner,sceneFor,sessionFor,str,reply,fail} from '@/lib/server';
import type {Session} from '@/lib/types';
export async function POST(req:Request){try{const user=await owner();const data=await body(req);const scene=await sceneFor(str(data.sceneId,100),user);if(data.parentSessionId){const parent=await sessionFor(str(data.parentSessionId,100),user);if(parent.scene.id!==scene.id)throw new Error('scene mismatch');}const now=new Date().toISOString();const s:Session={id:crypto.randomUUID(),scene,goal:str(data.goal,600),status:'active',createdAt:now,updatedAt:now,version:0,turns:[{id:crypto.randomUUID(),opponent:scene.opening,attempts:[]}],branches:[],...(data.parentSessionId?{parentSessionId:data.parentSessionId}:{})};await db().prepare('INSERT INTO sessions (id, owner, scene_id, title, direction, status, mode, data, version, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)').bind(s.id,user,scene.id,scene.title,scene.direction,s.status,scene.mode,JSON.stringify(s),0,now,now).run();return reply(s,201);}catch(e){return fail(e);}}
